const circle = d3.select('circle');
const rect = d3.select('rect');

const circleRemoved = circle.remove();
const rectRemoved = rect.remove();

console.log(circleRemoved);
console.log(rectRemoved);


